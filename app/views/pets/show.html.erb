<h1><%= @pet.name %></h1>
<p>Specie: <%= @pet.specie.name if @pet.specie %></p>
<p>Owner: <%= @pet.owner.name if @pet.owner %></p>

<%= link_to 'Edit', edit_pet_path(@pet) %> |
<%= link_to "Delete", pet_path(@pet), data: { turbo_method: "delete" } %>
<%= link_to 'Back', pets_path %>
